import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  screenContainer: {
    flex: 1,
    padding: 10,
  },
  deleteButton: {
    borderRadius: 0,
    alignItems: 'flex-start',
  },
});
